/**
 * @file app.js
 * @author swan
 */

/* globals swan */

require('./subpackages/pkgship/app')
require('./subpackages/pkgship0/app.js')

App({
    onLaunch(options) {
        // do something when launch
    },
    onShow(options) {
        // do something when show
    },
    onHide() {
        // do something when hide
    }
});
